import { Button } from "@/components/ui/button";

const DashboardPage = () => {
  return (
    <Button variant="outline" size="lg">
      My Button
    </Button>
  );
};

export default DashboardPage;
